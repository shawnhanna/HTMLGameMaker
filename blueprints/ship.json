{
	"tag" : "ship",
	"transform":
	{
		"Position":
		{
			"x":"0",
			"y":"0"
		}
	},
	"texture":
	{
		"src": "img.png"
	},
	"functs":
	{
		"OnCollide": "if (collider.tag=='enemy') {this.doRemove = true;}",
		"OnUpdate": "if (Input.getKey(65)){this.transform.Position.x -= 3;}if (Input.getKey(87)){this.transform.Position.y -= 3;}if (Input.getKey(83)){this.transform.Position.y += 3;}if (Input.getKey(68)){this.transform.Position.x += 3;} if (Input.getKey(74)) {var v = GameObjectFactory(\"bullet.json\");}",
		"OnInit" : ""
	}
}